const selectSingle6 = document.querySelector('.burger-menu__line');
const selectSingle_title6 = selectSingle6.querySelector('.burger-menu__arrow');

selectSingle6.addEventListener('click', () => {
	if ('active' === selectSingle6.getAttribute('data-state')) {
		selectSingle6.setAttribute('data-state', '');
	} else {
		selectSingle6.setAttribute('data-state', 'active');
	}
});

const selectSingle = document.querySelector('.footer-adaptive__btn');
const selectSingle_title = selectSingle.querySelector('.footer-adaptive__arrow');

selectSingle.addEventListener('click', () => {
	if ('active' === selectSingle.getAttribute('data-state')) {
		selectSingle.setAttribute('data-state', '');
	} else {
		selectSingle.setAttribute('data-state', 'active');
	}
});


const select = document.querySelector('.footer-adaptive__btn2');
const select_title = select.querySelector('.footer-adaptive__arrow2');

select.addEventListener('click', () => {
	if ('active' === select.getAttribute('data-state')) {
		select.setAttribute('data-state', '');
	} else {
		select.setAttribute('data-state', 'active');
	}
});


const selectSingle3 = document.querySelector('.footer-adaptive__btn3');
const selectSingle_title3 = selectSingle3.querySelector('.footer-adaptive__arrow3');

selectSingle3.addEventListener('click', () => {
	if ('active' === selectSingle3.getAttribute('data-state')) {
		selectSingle3.setAttribute('data-state', '');
	} else {
		selectSingle3.setAttribute('data-state', 'active');
	}
});

const selectSingle4 = document.querySelector('.footer-adaptive__btn4');
const selectSingle_title4 = selectSingle4.querySelector('.footer-adaptive__arrow4');

selectSingle4.addEventListener('click', () => {
	if ('active' === selectSingle4.getAttribute('data-state')) {
		selectSingle4.setAttribute('data-state', '');
	} else {
		selectSingle4.setAttribute('data-state', 'active');
	}
});

const selectSingle5 = document.querySelector('.footer-adaptive__btn5');
const selectSingle_title5 = selectSingle5.querySelector('.footer-adaptive__arrow5');

selectSingle5.addEventListener('click', () => {
	if ('active' === selectSingle5.getAttribute('data-state')) {
		selectSingle5.setAttribute('data-state', '');
	} else {
		selectSingle5.setAttribute('data-state', 'active');
	}
});



